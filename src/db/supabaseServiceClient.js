/**
 * Supabase service client — temporarily disabled.
 * Loading `@supabase/supabase-js` was failing at boot (module export mismatch with @supabase/auth-js).
 * Re-enable by restoring createClient + resolveSupabaseSecretKey logic when deps are aligned.
 */
export default null
