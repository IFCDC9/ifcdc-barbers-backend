import ScheduleControlsScreen from "../schedule/ScheduleControlsScreen";

/** Admin → Schedule controls (super admin sees all shop barbers). */
export default function AdminScheduleScreen() {
  return <ScheduleControlsScreen />;
}
