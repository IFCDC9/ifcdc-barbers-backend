# Native projects are generated by `expo prebuild` / EAS Build; do not commit.
android/
ios/

.expo/
dist/

# Local Android signing (never commit real secrets)
credentials.json
*.jks
*.keystore
keys/
