# Simulator (recommended): http://127.0.0.1:10000 — backend must listen on 0.0.0.0:10000 (npm run dev).
# Physical device on Wi‑Fi: http://YOUR_MAC_LAN_IP:10000 (same host as Expo QR, not localhost).
EXPO_PUBLIC_API_URL=http://127.0.0.1:10000
EXPO_PUBLIC_BACKEND_URL=http://127.0.0.1:10000
